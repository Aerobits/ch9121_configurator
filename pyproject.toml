[project]
name = "ch9121-configurator"
version = "0.1.0"
description = "Configuration tool for CH9121 chip"
readme = "README.md"
requires-python = "~=3.8"
dependencies = [
    "netifaces>=0.11.0",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.8.6",
]
